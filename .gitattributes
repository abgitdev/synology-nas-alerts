# Normalize line endings. Force LF so shell/python scripts run correctly on
# Synology DSM / Linux regardless of the contributor's OS.
* text=auto eol=lf
*.py  text eol=lf
*.sh  text eol=lf
*.md  text eol=lf
*.yml text eol=lf
